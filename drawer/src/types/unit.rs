pub struct MusicUnit {
    pub title: String,
    pub artist: String,
    // Todo:
    // Use duration specific time rather than String
    pub duration: String,
}

pub struct PlaylistUnit {
    pub title: String,
    pub creator: String,
    pub song_count: usize,
}
pub struct ArtistUnit;


